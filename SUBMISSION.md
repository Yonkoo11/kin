# <PROJECT> — submission

**<One sentence: what it does → on whose technology → the mechanism → a measured result or a
liveness claim.>**

Model it on this shape, which is how the studied builders write every description:

> Adversarial mutation testing for AI applications: injects 50 seeded bugs, measures what your
> suite actually catches, and auto-heals the gaps with tests proven to kill.
> **87% coverage caught 48% — healed to 98% in ~10s, $0.**

End on proof. A number, or "live on mainnet". Never end on an adjective.

## The problem

<Who loses what today, and roughly what it costs them. One paragraph.>

## What we built

<The mechanism. Three to five sentences. Name the sponsor technology at the load-bearing step.>

## What is actually running

- Live: `<URL>`
- Deployed: `<contract / service>` on `<network>`
- Tier in the demo: `<which backend>` — and say so plainly if it is degraded

## Proof

| claim | evidence |
|---|---|
| <claim> | <link, tx hash, log, or measurement> |

## What it does not do yet

<Stated plainly. Judges trust a submission that draws its own boundary more than one that does not.>

## Sponsor integration depth

<Which specific APIs, at which step, and what breaks if you remove them. If nothing breaks, the
integration is decorative, and a judge who reads code will see that.>
