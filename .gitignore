# Secrets — never commit
.env
.env.*
.envrc
*.key
*.pem
keystore/
secrets/
.private/

# Foundry artifacts (broadcast/ contains tx data with deployer addresses; review before publishing)
contracts/cache/
contracts/out/
contracts/broadcast/
contracts/.gas-snapshot

# Hardhat (if used)
artifacts/
cache_hardhat/

# Anchor / Solana (if used)
.anchor/
target/
test-ledger/

# Node / Next
node_modules/
.next/
dist/
.turbo/

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Local deployment artifacts that may carry operator addresses
deployments/local-*.json

# Internal planning / working notes — never published. Public docs go in docs/.
ai/
.private/

# Convex generates these on every run
convex/_generated/
dist/

# Internal: strategy, demand evidence, personal background. Never published.
company/

# Vendored agent skills installed by tooling, not project source.
# They also carry key-generation examples that trip secret scanners.
.agents/
.claude/

# Agent instruction files: they describe how the author works, not how the app works.
CLAUDE.md
AGENTS.md
.claudeignore
.ralph/

# Unfilled entry templates. They go public only once they say something.
PROJECT_SPEC.md
INVARIANTS.md
SUBMISSION.md
VIDEO_SCRIPT.md
SOCIAL_POSTS.md
tsconfig.tsbuildinfo
.shots/
